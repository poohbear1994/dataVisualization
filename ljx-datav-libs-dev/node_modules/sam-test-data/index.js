export const a = Math.floor(Math.random() * 10)
export const b = Math.floor(Math.random() * 100)
export function random(base) {
  if (base && base % 1 === 0) {
    return Math.floor(Math.random() * base) 
  } else {
    return 0
  }
}
